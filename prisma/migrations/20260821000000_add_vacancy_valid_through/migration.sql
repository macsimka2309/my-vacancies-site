-- Срок действия объявления для микроразметки (`validThrough`).
-- Объявление без срока агрегаторы отбраковывают при импорте, а Яндекс убирает
-- из блока вакансий.
--
-- Колонка намеренно остаётся пустой: срок по умолчанию считается от updated_at
-- (+60 дней) в lib/vacancy-validity.ts, поэтому любая правка вакансии его
-- продлевает сама. Заполненное значение — это ручное «закрывается такого-то».
ALTER TABLE "vacancies" ADD COLUMN "valid_through" DATE;
