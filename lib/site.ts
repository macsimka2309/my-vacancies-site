// Централизованные данные о сайте/операторе.
type SiteConfig = {
  name: string;
  legalName: string;
  inn: string;
  email: string;
  url: string;
  tagline: string;
  phone: string;
  /** Обещание обратной связи: разное в рабочие часы и ночью. */
  callback: {
    /** Часы приёма звонков по Москве, включительно от fromHour до toHour. */
    fromHour: number;
    toHour: number;
    /** Текст в рабочие часы. */
    duringHours: string;
    /** Текст вне рабочих часов — обещать быстрый звонок ночью нечестно. */
    afterHours: string;
  };
  /** Ник получателя в Telegram (без @). */
  telegram: string;
  /** Ссылка на профиль-получателя в мессенджере MAX (открывает приложение, если установлено). */
  max: string;
  /** Ключ IndexNow. Публичный по устройству протокола — он же лежит в public/. */
  indexNowKey: string;
};

export const site: SiteConfig = {
  /** Отображаемое название бренда (шапка, футер, заголовки вкладок). */
  name: "Работа Рядом",
  /** Полное наименование оператора персональных данных. */
  legalName: "Салихов Руслан Эркинович",
  /** ИНН оператора (для футера и политики конфиденциальности). */
  inn: "525689929260",
  /** Контакт оператора для обращений по персональным данным. */
  email: "macsimnn@mail.ru",
  /** Канонический адрес сайта (для метаданных и микроразметки). */
  url: "https://my-dream-vacancy.ru",
  /** Короткий слоган над заголовком. */
  tagline: "Работа в стабильной компании",
  /** Контактный телефон (пустая строка — блок и кнопка «Позвонить» скрыты). */
  phone: "+7 996 567-03-03",
  /**
   * Обещание обратной связи. Исследования скорости ответа на заявки: чем
   * быстрее звонок, тем выше конверсия, но обещать ночной звонок нельзя —
   * поэтому текст зависит от времени суток. Меняйте под реальный график.
   */
  callback: {
    fromHour: 9,
    toHour: 21,
    duringHours: "Перезвоним в течение часа",
    afterHours: "Перезвоним утром, после 9:00",
  },
  /** Ник получателя в Telegram (без @). */
  telegram: "HR_Russslan0303",
  /** Ссылка на профиль-получателя в MAX (universal link — открывает приложение, если установлено). */
  max: "https://max.ru/u/f9LHodD0cOKBCVZGavwAZXHhEOgBXIZeWI4UJ840qz1RB5a7UCHuMYcuvNg",
  /**
   * Ключ IndexNow — им поисковик проверяет, что сайтом владеем мы.
   * Не секрет: файл с этим же значением лежит в public/ и открыт всем,
   * так протокол и устроен. При смене ключа переименовать и файл —
   * расхождение ловит тест tests/unit/indexnow.test.ts.
   */
  indexNowKey: "dae0cfa8658c78d1e623c56be5a0294f",
};
